---

---

<section class="hero">
  <div class="container">
    <div class="hero-content">
      <div class="logo-container">
        <img
          src="/logo_hero.webp"
          alt="Logo BLS Servicios Audiovisuales"
          class="hero-logo"
          width="300"
          height="300"
        />
      </div>
      <h1>Soluciones técnicas profesionales para tus eventos</h1>
      <p>
        Iluminación, sonido, video y escenarios para todo tipo de eventos.
        Hacemos que tu evento brille con equipamiento de última generación.
      </p>
      <div class="hero-buttons">
        <a
          href="#contacto"
          class="btn btn-primary"
          aria-label="Contáctanos para solicitar servicios audiovisuales"
          >Contáctanos</a
        >
        <a
          href="#servicios"
          class="btn btn-secondary"
          aria-label="Ver nuestros servicios audiovisuales"
          >Nuestros Servicios</a
        >
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    background: linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.7)),
      url("/hero-background.webp");
    background-size: cover;
    background-position: center;
    color: white;
    height: 100vh;
    display: flex;
    align-items: flex-start; /* Cambiado de center a flex-start para alinear arriba */
    padding-top: 10vh; /* Añadido espacio superior */
    text-align: center;
    padding-bottom: 0; /* Eliminado el padding-bottom */
  }

  .hero-content {
    max-width: 800px;
    margin: 0 auto;
    padding: 0 1rem;
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
  }

  .logo-container {
    margin-bottom: 2rem;
    margin-top: -3rem; /* Mover el logo más arriba */
  }

  .hero-logo {
    max-width: 500px;
    height: auto;
    filter: drop-shadow(0px 0px 10px rgba(255, 255, 255, 0.5));
  }

  .hero h1 {
    font-size: 3rem;
    margin-bottom: 1rem;
    line-height: 1.2;
  }

  .hero p {
    font-size: 1.2rem;
    margin-bottom: 1.5rem;
    opacity: 0.9;
  }

  .hero-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
  }

  @media (max-width: 768px) {
    .hero {
      padding-top: 15vh; /* Más espacio superior en móvil */
    }

    .hero-logo {
      max-width: 200px; /* Tamaño reducido para móviles */
    }

    .logo-container {
      margin-bottom: 1rem;
      margin-top: -1rem; /* Ajustar margen superior en móvil */
    }

    .hero h1 {
      font-size: 2.2rem;
    }

    .hero p {
      font-size: 1rem;
    }

    .hero-buttons {
      flex-direction: column;
      align-items: center;
    }
  }

  /* Para dispositivos aún más pequeños */
  @media (max-width: 480px) {
    .hero-logo {
      max-width: 160px; /* Aún más pequeño para móviles pequeños */
    }

    .hero h1 {
      font-size: 1.8rem;
    }
  }
</style>
